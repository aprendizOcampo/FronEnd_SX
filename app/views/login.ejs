<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
  <title>log in</title>
</head>
<body class="position-absolute top-50 start-50 translate-middle">
  <div class="text-center">
    <h2>Ingresar como Administrador</h2>
    <p class="text-muted">Recuerda que si no tienes un usuario administrador, no podrás acceder a tu inventario.</p>
  </div>

  <div class="container-sm">
    <main class="form-signin text-center shadow-lg p-3 mb-5">
      <form id="formLogin" action="/login" method="POST">
        <div class="form-outline mb-4">
          <input type="text" class="form-control" id="usuario" name="usuario" placeholder="usuario">
        </div>
        <div class="form-outline mb-4">
          <input type="password" class="form-control" id="password" name="password" placeholder="contraseña">
        </div>
        <button class="w-100 btn btn-primary" type="submit">Ingresar</button>    
      </form>

      <br>
      <div class="text-center">
        <p>¿No has creado un usuario administrador? <a href="/register">¡crea uno!</a></p>
      </div>
    </main>
  </div>

    <script src="//cdn.jsdelivr.net/npm/sweetalert2@11">
  <% if(alert) { %>
        title: '<%= alertTitle %>',
        text:'<%= alertMessage %>',
        icon:'<%= alertIcon %>',
        showConfirmButton: <%= showConfirmButton %>,
        timer: <%= timer %>
      }).then(()=>{
        window.location = '/<%= ruta %>'
      })
      <% } %>
</script>
</body>
</html>
